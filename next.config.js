// @ts-check

const withImages = require("next-images");

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
    /* config options here */
};

module.exports = withImages(nextConfig);
