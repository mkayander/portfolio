import React from "react";

const initialState = {};

const cvInfoReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

/*
    Actions
*/

/*
    Selectors
*/

export default cvInfoReducer;
